/** Un formato guardado por el usuario (instrucción reutilizable, ver brief "Formatos" 2026-07-13). */
export type AiRecipe = {
  id: string;
  name: string;
  instruction: string;
  isDefault: boolean;
  createdAt: string;
};
